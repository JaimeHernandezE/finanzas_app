[pytest]
DJANGO_SETTINGS_MODULE = core.settings
python_files = tests/*.py tests/**/*.py
python_classes = Test*
python_functions = test_*
addopts = -ra
# Los tests deben ejecutarse con: docker-compose exec web pytest tests/ -v
# o backend/scripts/run_tests.ps1 — nunca python -c contra finanzas_db.
